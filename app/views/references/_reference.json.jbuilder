json.extract! reference, :id, :tag, :project, :country_city, :address, :address_link, :installer, :installer_link, :owner, :owner_link, :created_at, :updated_at
json.url reference_url(reference, format: :json)
