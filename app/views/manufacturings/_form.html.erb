<%= form_with(model: manufacturing, local: true) do |form| %>
  <% if manufacturing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manufacturing.errors.count, "error") %> prohibited this manufacturing from being saved:</h2>

      <ul>
      <% manufacturing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :inputs %>
    <%= form.text_field :inputs %>
  </div>

  <div class="field">
    <%= form.label :process %>
    <%= form.text_field :process %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
