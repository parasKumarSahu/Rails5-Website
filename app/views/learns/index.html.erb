<p id="notice"><%= notice %></p>

<h1>Learns</h1>

<table>
  <thead>
    <tr>
      <th>Video link</th>
      <th>Thumbnail</th>
      <th>Thumbnail type</th>
      <th>Thumbnail size</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @learns.each do |learn| %>
      <tr>
        <td><%= learn.video_link %></td>
        <td><%= learn.thumbnail %></td>
        <td><%= learn.thumbnail_type %></td>
        <td><%= learn.thumbnail_size %></td>
        <td><%= link_to 'Show', learn %></td>
        <td><%= link_to 'Edit', edit_learn_path(learn) %></td>
        <td><%= link_to 'Destroy', learn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Learn', new_learn_path %>
