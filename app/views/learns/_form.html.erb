<%= form_with(model: learn, local: true) do |form| %>
  <% if learn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(learn.errors.count, "error") %> prohibited this learn from being saved:</h2>

      <ul>
      <% learn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :video_link %>
    <%= form.text_field :video_link %>
  </div>

  <div class="field">
    <%= form.label :thumbnail %>
    <%= form.text_field :thumbnail %>
  </div>

  <div class="field">
    <%= form.label :thumbnail_type %>
    <%= form.text_field :thumbnail_type %>
  </div>

  <div class="field">
    <%= form.label :thumbnail_size %>
    <%= form.number_field :thumbnail_size %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
