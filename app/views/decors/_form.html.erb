<%= form_with(model: decor, local: true) do |form| %>
  <% if decor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(decor.errors.count, "error") %> prohibited this decor from being saved:</h2>

      <ul>
      <% decor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :image_path %>
    <%= form.text_field :image_path %>
  </div>

  <div class="field">
    <%= form.label :image_type %>
    <%= form.text_field :image_type %>
  </div>

  <div class="field">
    <%= form.label :image_size %>
    <%= form.number_field :image_size %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
